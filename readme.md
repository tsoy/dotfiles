```
$ chezmoi add ~/.bashrc
$ chezmoi edit ~/.bashrc

$ chezmoi diff
$ chezmoi -v apply
$ chezmoi merge $FILE
$ chezmoi update -v

$ chezmoi add --autotemplate ~/.gitconfig
$ chezmoi chattr +template ~/.zshrc
```
